import sql => imfdb1.sql , radius.sql

edit "config.php" in "Application > config > config.php";

== in config.php ==

change $config['base_url'] to be your host;


-------------------------

edit "database.sql" in "Application > config > database.php";

setting $db to same like your host;

